"""
# Tests booleen

Chaque type de Python peut être evalue dans un test booleen. Les entiers par exemple valent
toujours vrai, sauf pour `0`. Ainsi, la pluspart des types primitifs de python possedent 
une valeur fausse; l'objectif de cet exercice est de les lister.

Modifiez les valeurs de `a`, `b`, `c`, `d`, `e`, `f` et `g` jusqu'a ne plus avoir d'erreur.

[Doc: Tests booleen](https://docs.python.org/3/library/stdtypes.html#truth-value-testing)
"""

# TODO
a = ...
b = ...
c = ...
d = ...
e = ...
f = ...
g = ...

# Do not touch!
assert isinstance(a, bool)    and not a
assert isinstance(b, int)     and not b
assert isinstance(c, complex) and not c
assert isinstance(d, str)     and not d
assert isinstance(e, list)    and not e
assert isinstance(f, tuple)   and not f
assert isinstance(g, dict)    and not g
